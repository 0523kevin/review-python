#math
import math
ratio = signal_power / noise_power
decibels = 10*math.log10(ratio)

radians = 0.7
height = math.sin(radians)

math.sqrt(2)
>>> root 2

#random
import random

for i in range(10):
    x = random.random()
    print(x)

random 은 0.0 부터 1.0 미만 까지의 수를 랜덤으로 반환한다

t = [1,2,3]
random.choice(t)
